/* Maths Brahmastra — site settings.
   On a normal GitHub Pages address (https://USERNAME.github.io/REPOSITORY/) leave everything empty:
   the Board Papers page works out the user name and repository from the address by itself.
   Fill these in only if you use a custom domain or the list of documents does not load. */
window.MBX_CONFIG = {
  githubUser: "",      // e.g. "drpk-maths"
  githubRepo: "",      // e.g. "class10-maths"
  branch: "",          // leave empty for the default branch (main)
  papersDir: "papers"  // folder in the repository that holds the uploaded documents
};
